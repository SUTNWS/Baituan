使用redis/mongodb存放题库，mysql存放学生信息（学院，班级，学号，姓名，成绩等）
操作系统使用linux，使用nginx进行反向代理和并发处理

设计思路：
后端开启php-redis/php-mongodb拓展，使用随机抽取的方式在nosql数据库中抽取题目，返回给前端
前端收集用户答案，以json数组的形式请求后端接口，接口接收所有题目答案后，按照题目id在答案库中比对答案并评分
将最终分数返回给前端让前端显示分数，并将分数插入对应学生的分数字段
